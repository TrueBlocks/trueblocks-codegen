import { Stack, Text } from '@mantine/core';

export const Home = () => {
  return (
    <Stack
      style={{ height: '100%', backgroundColor: 'yellow', color: 'black' }}
    >
      <Text>THIS IS THE HOME SCREEN</Text>
    </Stack>
  );
};
